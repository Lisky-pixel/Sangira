// TODO: swap for final brand asset if the motif is delivered separately
import pendingVerificationBanner from '../assets/placeholder/pending-verification-banner.png'

export const pendingVerificationImages = {
  banner: pendingVerificationBanner,
} as const
