Shared UI components live here.

- `layout/PublicHeader` — public top nav with Radix mobile drawer below `lg`
- `layout/HeaderMinimal` — minimal header for registration and auth flows
- `layout/Footer` — shared app footer
- `layout/ContentGrid` — responsive grid helper
- `landing/*` — landing page section components
- `register/*` — registration flow components
- `ui/button` — primary / outline / ghost button variants
- `ui/step-pill` — step indicator for multi-step flows
