// Swappable mock data — replace imports with live API services one screen at a time.
export type { Listing } from '../types/listing'

export const placeholderListings: import('../types/listing').Listing[] = []
